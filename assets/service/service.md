# Explanation

These localization file under `service` folder is not included to the example project.

After you initialize your own Firebase Storage for testing, upload these files to there under `localizations` file.

Setup your own firebase (id/token etc.) in .env files and Run the project to test it yourself.
